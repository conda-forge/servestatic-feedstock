context:
  version: "4.3.2"

package:
  name: servestatic
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/s/servestatic/servestatic-${{ version }}.tar.gz
    sha256: e6142e53c849d1f22c44e482ee25c1c61b60ae493b3d3584c628ed70d157a69b

build:
  number: 0
  script: ${{ PYTHON }} -m pip install .
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }},<4.0.0
    - asgiref
    - brotli

tests:
  - python:
      imports:
        - servestatic
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/Archmonger/ServeStatic/
  summary: Production-grade static file server for Python WSGI & ASGI.
  license: MIT
  license_file: LICENSE.md
  documentation: https://archmonger.github.io/ServeStatic/

extra:
  recipe-maintainers:
    - carlodri
